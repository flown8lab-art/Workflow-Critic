🔧 Техническая архитектура агрегации (3 компонента)
1. Сбор данных из источников (Data Collection)
Hirify использует комбинацию методов для охвата 1500+ источников:

A) Парсинг Telegram-каналов
Как работает:

Используется Telegram API (либо официальный Bot API, либо MTProto через библиотеки типа Telethon/Pyrogram)

Бот подписывается на ~200-500 каналов рекрутеров и IT-сообществ (например, "Вакансии IT", "Remote работа", "Relocate jobs")
​

Каждый N минут (или в реальном времени через webhook) считывает новые сообщения

Технический пример (Python + Telethon):

python
from telethon import TelegramClient

API_ID = 'your_api_id'
API_HASH = 'your_api_hash'
client = TelegramClient('session', API_ID, API_HASH)

async def parse_channels():
    channels = ['@remote_jobs_ru', '@it_vacancies', '@relocate_channel']
    
    for channel in channels:
        async for message in client.iter_messages(channel, limit=50):
            if message.text and ('вакансия' in message.text.lower() or 'hiring' in message.text.lower()):
                job_data = extract_job_info(message.text)
                save_to_db(job_data)
Что парсится:

Текст сообщения (название роли, компания, требования, контакты)

Дата публикации

Хэштеги (#remote, #senior, #python)

Ссылки на внешние ресурсы
​

B) Парсинг сайтов компаний (Web Scraping)
Как работает:

Используются скрейперы (Scrapy/Playwright/Puppeteer) для обхода карьерных страниц компаний

Целевые URL: company.com/careers, company.com/jobs, company.com/vacancies

Скрейпер извлекает: название роли, описание, требования, дату публикации, форму отклика
​

Архитектура:

text
Crawler → HTML Parser → Data Extractor → Database
    ↓           ↓              ↓              ↓
  Requests  BeautifulSoup   Regex/NLP    PostgreSQL
  Playwright   lxml        OpenAI API    + ElasticSearch
Пример (упрощённый):

python
import requests
from bs4 import BeautifulSoup

def scrape_company_jobs(company_url):
    response = requests.get(f"{company_url}/careers")
    soup = BeautifulSoup(response.content, 'html.parser')
    
    job_cards = soup.find_all('div', class_='job-card')
    
    for card in job_cards:
        title = card.find('h3').text
        location = card.find('span', class_='location').text
        link = card.find('a')['href']
        
        save_job({
            'title': title,
            'location': location,
            'link': link,
            'source': company_url
        })
Проблемы и решения:

Anti-scraping защита → IP-ротация через прокси, headless browsers (Playwright/Puppeteer), задержки между запросами

Изменения структуры сайта → регулярный мониторинг и обновление селекторов, либо использование AI для адаптивного парсинга
​

CAPTCHA → решение через сервисы (2Captcha, Anti-Captcha) или избежание через rate limiting
​

C) Интеграция с API джоб-бордов
Как работает:

Прямая интеграция через официальные API (Lever, Greenhouse, Workable, BambooHR)

Это структурированные данные в JSON/XML формате — самый надёжный источник
​

Пример API-запроса (Lever):

python
import requests

LEVER_API = "https://api.lever.co/v0/postings/company-name"

response = requests.get(LEVER_API)
jobs = response.json()

for job in jobs:
    save_job({
        'title': job['text'],
        'location': job['categories']['location'],
        'description': job['description'],
        'source': 'Lever',
        'apply_url': job['applyUrl']
    })
Плюсы API:

✅ Легально и стабильно

✅ Структурированные данные (не нужен парсинг HTML)

✅ Авто-обновления
​

Минусы:

❌ Ограниченная доступность (не все компании используют публичные ATS)

❌ Rate limits (например, 100 запросов/час)

❌ Требует аутентификации/партнёрства
​

D) RSS-фиды
Как работает:

Многие джоб-борды публикуют RSS/Atom фиды с вакансиями
​

Парсер подписывается на фиды и проверяет их каждые 15-60 минут

Пример (Python + feedparser):

python
import feedparser

RSS_FEED = "https://some-job-board.com/rss/jobs"

feed = feedparser.parse(RSS_FEED)

for entry in feed.entries:
    save_job({
        'title': entry.title,
        'link': entry.link,
        'published': entry.published,
        'source': 'RSS Feed'
    })
2. Обработка и нормализация данных (Data Processing)
Проблема: Сырые данные из 1500 источников неоднородны (разные форматы, дубликаты, опечатки, мусор).

Решение: Pipeline обработки

A) Дедупликация (удаление дубликатов)
Алгоритм: Fuzzy matching по названию роли + компании + локации

Инструменты: Levenshtein distance, SimHash, MinHash
​

python
from fuzzywuzzy import fuzz

def is_duplicate(job1, job2):
    title_match = fuzz.ratio(job1['title'], job2['title'])
    company_match = fuzz.ratio(job1['company'], job2['company'])
    
    if title_match > 85 and company_match > 90:
        return True
    return False
B) Нормализация
Job title: "Software Engineer" = "Software Developer" = "SWE"

Локация: "Москва" → "Moscow, Russia", "SF" → "San Francisco, USA"
​

Зарплата: "$100k-120k" → structured {min: 100000, max: 120000, currency: 'USD'}
​

C) Извлечение тегов/скиллов (NLP/LLM)
Из описания вакансии извлекаются теги: #python, #remote, #senior, #b2b
​

Используется либо NLP (spaCy, NLTK), либо LLM (GPT-4 с промптом "extract skills from JD")
​

python
# Пример с OpenAI API
import openai

def extract_skills(job_description):
    prompt = f"Extract technical skills from this job description: {job_description}"
    response = openai.ChatCompletion.create(
        model="gpt-4",
        messages=[{"role": "user", "content": prompt}]
    )
    return response.choices[0].message.content
D) Классификация
Seniority: Junior/Middle/Senior/Lead по ключевым словам в JD
​

Work mode: Remote/Hybrid/Onsite по наличию "remote"/"office"/"hybrid"
​

Domain: Fintech/SaaS/E-commerce по компании или описанию
​

3. Хранение и индексация (Database & Search)
База данных:

PostgreSQL для реляционных данных (вакансии, компании, пользователи)

ElasticSearch для полнотекстового поиска и фильтрации (быстрый поиск по 50k+ вакансий)
​

Redis для кэширования популярных запросов
​

Схема данных (упрощённая):

sql
CREATE TABLE jobs (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255),
    company VARCHAR(255),
    description TEXT,
    location VARCHAR(255),
    salary_min INT,
    salary_max INT,
    currency VARCHAR(10),
    seniority VARCHAR(50),
    work_mode VARCHAR(50),
    tags TEXT[], -- {python, remote, b2b}
    source VARCHAR(100),
    source_url TEXT,
    published_at TIMESTAMP,
    expires_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_tags ON jobs USING GIN(tags);
CREATE INDEX idx_location ON jobs(location);
CREATE INDEX idx_seniority ON jobs(seniority);
Поисковый индекс (ElasticSearch):

json
{
  "mappings": {
    "properties": {
      "title": {"type": "text"},
      "description": {"type": "text"},
      "tags": {"type": "keyword"},
      "location": {"type": "keyword"},
      "seniority": {"type": "keyword"},
      "published_at": {"type": "date"}
    }
  }
}
⚙️ Автоматизация и масштабирование
Scheduled Tasks (cron jobs)
Каждые 15 минут: парсинг Telegram-каналов и RSS
​

Каждые 1 час: парсинг топ-100 компаний
​

Каждые 6 часов: парсинг всех ~1500 источников
​

Каждые 24 часа: удаление истёкших вакансий (older than 30 days)
​

Proxy rotation
Для избежания блокировок используются ротирующиеся прокси (Bright Data, Oxylabs, Smartproxy)

IP меняется каждые 10-50 запросов
​

Cloud infrastructure
Парсеры запускаются на серверах (AWS EC2, DigitalOcean) или serverless (AWS Lambda для небольших задач)
​

Очереди задач (RabbitMQ/Celery) для распределения парсинга по воркерам
​

🛠️ Как это сделать для твоего продукта (упрощённая версия)
Если тебе не нужен "весь рынок", а нужно агрегировать только релевантные вакансии для твоей ниши (например, Product Manager в B2B SaaS):

Минималистичный стек (MVP):
1. Парсинг Telegram (50-100 каналов):

Python + Telethon

Запуск каждые 30 минут (cron job на VPS)

Сохранение в PostgreSQL

2. Парсинг 10-20 целевых сайтов:

Python + Playwright (для JS-heavy сайтов)

Целевые компании: Stripe, Notion, Linear, Miro, Figma (career pages)

3. API интеграции (опционально):

Hirify API (если дадут партнёрство)

Lever/Greenhouse публичные фиды

4. LLM для обработки:

Claude API для извлечения требований из JD

Теги и категоризация автоматически

Бюджет инфраструктуры (первые 3 месяца):

VPS (DigitalOcean Droplet): $12/мес

Proxy (Bright Data Starter): $25/мес

OpenAI/Claude API: $50-100/мес

PostgreSQL (Supabase Free Tier): $0

Итого: ~$100-150/мес

🎯 Выводы для твоего продукта
Нужна ли тебе агрегация вакансий?

Зависит от позиционирования:

Вариант A: Агрегатор НЕ нужен (фокус на tailoring)

Пользователь сам находит вакансии (LinkedIn/сайты компаний/Telegram)

Твой продукт — это "вторая половина" (tailoring + версионирование + cover letters)

Проще запуститься, меньше инфраструктуры

Вариант B: Лёгкая агрегация (hybrid)

Парсишь только Telegram-каналы (200-300 релевантных для твоей ниши)

Показываешь вакансии в продукте → пользователь кликает "Create resume for this job" → запускается tailoring

Больше value для пользователя, но +2 недели на разработку

Вариант C: Полная агрегация (как Hirify)

Собираешь 20k+ вакансий из всех источников

Это отдельный продукт (джоб-борд), требует команду и инфраструктуру

Долго и дорого для MVP

Моя рекомендация для твоего MVP:
Вариант A (без агрегации) → быстрее запуск, фокус на tailoring quality.
Через 2-3 месяца можешь добавить Вариант B (Telegram-парсинг) как бонусную фичу.

Хочешь, разберём конкретно как сделать Telegram-парсер для твоей ниши (Product/B2B SaaS вакансии), или сразу возвращаемся к проектированию MVP tailoring-платформы?